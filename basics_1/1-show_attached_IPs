#!/usr/bin/env bash
# Script to retrieve only the IPv4 addresses of active network interfaces (excluding loopback)

# Use 'ip a' to list all network interfaces and filter out the IPv4 addresses excluding loopback (127.x.x.x)
ip -4 -o addr show scope global | awk '{print $4}' | cut -d'/' -f1

